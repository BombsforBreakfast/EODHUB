/** Master shell switches to mobile layout at or below this width. */
export const MOBILE_SHELL_MAX = 900;

/** Desktop shell with side rails — auto-collapse rails below this width. */
export const COMPACT_DESKTOP_MAX = 1280;

/** When viewport shrinks to this fraction of the widest seen width, collapse rails. */
export const SHRINK_COLLAPSE_RATIO = 0.85;

/** Profile card / strip layouts stack below this width (includes narrow desktop columns). */
export const STACKED_PROFILE_MAX = 1100;
