import MasterShell from "../components/master/MasterShell";

export default function UserDirectoryLayout({ children }: { children: React.ReactNode }) {
  return <MasterShell>{children}</MasterShell>;
}
